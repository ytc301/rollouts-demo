apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: canary
spec:
  selector:
    matchLabels:
      app: canary-test
  template:
    spec:
      containers:
      - name: canary
        image: argoproj/rollouts-demo:latest
        imagePullPolicy: Always